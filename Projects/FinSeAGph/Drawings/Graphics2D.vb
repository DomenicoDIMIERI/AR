Namespace Drawings

    Public Class Graphics2D

        Sub setColor(color As JColor)
            Throw New NotImplementedException
        End Sub

        Sub fillRect(p1 As Integer, p2 As Integer, p3 As Integer, p4 As Integer)
            Throw New NotImplementedException
        End Sub

        Sub translate(p1 As Single, p2 As Single)
            Throw New NotImplementedException
        End Sub

        Sub rotate(p1 As Double)
            Throw New NotImplementedException
        End Sub

        Shared Function FromGraphics(graphics As Graphics) As Graphics2D
            Throw New NotImplementedException
        End Function

        Sub drawImage(baseImage As BufferedImage, p2 As Integer, p3 As Integer, p4 As Integer, p5 As Integer, p6 As Integer, p7 As Integer, p8 As Integer, p9 As Integer, p10 As Object)
            Throw New NotImplementedException
        End Sub

        Sub drawImage(baseImage As BufferedImage, p2 As Object, p3 As Integer, p4 As Integer)
            Throw New NotImplementedException
        End Sub

        Sub dispose()
            Throw New NotImplementedException
        End Sub

        Sub setComposite(p1 As Object)
            Throw New NotImplementedException
        End Sub

        Sub setRenderingHint(p1 As Object, p2 As Object)
            Throw New NotImplementedException
        End Sub

        Sub SetClip(shape As Shape)
            Throw New NotImplementedException
        End Sub

        Sub setPaint(nonStrokingPaint As Paint)
            Throw New NotImplementedException
        End Sub

        Sub fill(generalPath As GeneralPath)
            Throw New NotImplementedException
        End Sub

        Sub setStroke(newStroke As BasicStroke)
            Throw New NotImplementedException
        End Sub

        Function getStroke() As BasicStroke
            Throw New NotImplementedException
        End Function

        Sub draw(path As GeneralPath)
            Throw New NotImplementedException
        End Sub

        Sub drawImage(awtImage As BufferedImage, at As AffineTransform, p3 As Object)
            Throw New NotImplementedException
        End Sub

        Sub Transform(at As AffineTransform)
            Throw New NotImplementedException
        End Sub

        Sub drawGlyphVector(p1 As Object, p2 As Single, p3 As Single)
            Throw New NotImplementedException
        End Sub

        Sub setBackground(TRANSPARENT_WHITE As Color)
            Throw New NotImplementedException
        End Sub

        Sub clearRect(p1 As Integer, p2 As Integer, p3 As Integer, p4 As Integer)
            Throw New NotImplementedException
        End Sub

        Sub scale(scaling As Single, scaling1 As Single)
            Throw New NotImplementedException
        End Sub

        Shared Function FromImage(packedImage As BufferedImage) As Graphics2D
            Throw New NotImplementedException
        End Function

        
    End Class

End Namespace